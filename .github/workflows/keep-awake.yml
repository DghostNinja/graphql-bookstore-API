name: Keep Awake

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Wake machine
        run: curl -s --connect-timeout 10 https://graphql-bookstore.fly.dev/health